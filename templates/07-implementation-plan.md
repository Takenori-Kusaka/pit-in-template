# 実装計画

`specs/F-NNN/plan.md` として置きます。機能仕様承認(G-4)の判定対象です。

対象の機能仕様: `specs/F-NNN/spec.md`

## タスク

| ID | 内容 | 対応する受入基準 | 変更の対象パス | 依存 | 並列可 |
| --- | --- | --- | --- | --- | --- |
| Task-1 | | 1, 2 | `src/invite/**` | — | 可 |
| Task-2 | | 3 | `src/api/invite.ts` | Task-1 | 不可 |

**対応する受入基準のないタスクを作らないでください**。何のための作業か分からないタスクは、完了の判定ができません。

「変更の対象パス」は、実行環境の書き込み範囲の設定に使います。範囲外のファイルを変更する必要が出たら、**計画の誤りとして差し戻します**。エージェントが自分で範囲を広げてはなりません。

## 粒度の確認

| 項目 | 上限 | 見込み |
| --- | --- | --- |
| 変更行数 | 400 | |
| 変更ファイル数 | 15 | |
| レビュー所要時間 | 30分 | |

上限を超える見込みのタスクは分割します。**目安は行数ではなくレビュー可能性です**。

## テストの方針

| 受入基準 | テストの種類 | どこに置くか |
| --- | --- | --- |
| 1 | 単体 | |
| 2 | 結合 | |
| 3 | 単体 | |

テストは**実装より先に生成します**。実装コードをテスト生成の入力に渡しません。実装を読んだうえでテストを書くと、実装の誤りをそのまま正解とみなすテストが生成されます。

## スタック構成(依存する変更を積み上げる場合のみ)

| 層 | 対応タスク | ベース | 扱う関心事 |
| --- | --- | --- | --- |
| 1 | Task-1 | main | 1つだけ |
| 2 | Task-2 | 1層目 | 1つだけ |

**3層を超える場合は分割の設計を見直します**。マージは下から順に行います。

## AI エージェントへ与える分割の指示

実装計画の承認後、エージェントへ渡す指示です。**恒久層の指示資産と重複させません**。

- 1つの変更単位で1つの関心事のみを扱う
- 変更単位ごとに単独でビルドと自動検証が通る状態にする
- 目安100行を超えたら、分割の可否を人へ確認する
- 依存の順序は上表の層番号に従う

## 分解しすぎない

事前に細かく割り切ると、かえって手戻りが増えます。分解は、**依存関係と検証の単位が決まる粒度で止めます**。

## 承認(G-4)

| 項目 | 値 |
| --- | --- |
| 判定者 | 価値責任者(または委譲先) |
| 判定日 | |
| 結果 | |
