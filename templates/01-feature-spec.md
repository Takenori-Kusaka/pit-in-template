---
id: F-001
title:
owner:
status: draft
approved_by:
approved_at:
scope: SC-M
---

# F-001 <機能の名前>

`specs/F-001/spec.md` として置きます。

## 背景

誰の、どんな問題を解くのか。1〜3段落。

## 受入基準

EARS 記法で書きます。**曖昧語(適切に / 柔軟に / 可能な限り / など / 必要に応じて / 基本的に)は使えません**。CI の `spec-lint` が検出します。

| # | 型 | 受入基準 |
| --- | --- | --- |
| 1 | イベント駆動 | 招待の有効期限を過ぎた場合、システムは一覧に「失効」と表示しなければならない |
| 2 | 望ましくない事象 | 失効した招待に対して取り消しを実行した場合、システムは HTTP 409 を返さなければならない |
| 3 | ユビキタス | システムは招待トークンをログへ出力してはならない |

各基準には、**違反すると失敗するテストが1つ以上**必要です。

## スコープ外

書かれていない事項は、実装されるか否かが不定になります。やらないことを明記します。

- 招待の再発行(F-013 で扱う)
- 一括取り消し

## 前提

この機能が成り立つために、外部で満たされている必要のあること。崩れたら受入基準も変わります。

| # | 前提 | 崩れたときの影響 |
| --- | --- | --- |
| 1 | 招待テーブルに `expires_at` がある | 基準1が実装できない |

## 非機能の要求

受入基準に現れない性質は、ここで指定します。指定がなければ既定値です。

| 項目 | 要求 |
| --- | --- |
| 応答時間 | |
| 同時実行 | |
| 監査ログ | |

## 未確定事項

曖昧なまま残っている点を書きます。**G-2 / G-4 の通過には解消が必要**です。空にできる欄ではなく、**解消したら消す欄**です。

- [NEEDS CLARIFICATION] <確定していない点>

## コア指定

この機能はコアか。コアなら、人が理解を維持し続ける対象になります。

- [ ] コア機能である(ADR の添付が G-6 で必須になる)

## 承認(G-2 / G-4)

| 項目 | 値 |
| --- | --- |
| 判定者 | |
| 判定日 | |
| 結果 | 通過 / 差し戻し |
| 差し戻し理由 | 判定基準の項目番号で示す |
